Config = {

    devMode = {
        active = false, -- Set true to view debug prints
    },
    -----------------------------------------------------

    defaultlang = 'en_lang',
    -----------------------------------------------------

    keys = {
        Pickup = "G", -- Pickup Chest prompt
        Open = "B"    -- Open Chest prompt
    },
    -----------------------------------------------------

    notifications = {
        showNotifications = true, -- Set false to disable all core notifications
    },
    -----------------------------------------------------
}
